apiVersion: v1
kind: Service
metadata:
  name: crochelia
  namespace: crochelia
spec:
  type: NodePort
  selector:
    app: crochelia
  ports:
    - name: http
      port: 80
      targetPort: http
      nodePort: 30691
