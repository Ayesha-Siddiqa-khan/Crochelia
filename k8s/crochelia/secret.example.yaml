# Template only — never commit real values. Copy this file, fill it in,
# and apply it out-of-band (kubectl apply -f, or a secrets manager /
# External Secrets Operator). PROJECT.md §10.10.
#
#   cp secret.example.yaml secret.local.yaml   # .local. files are gitignored
#   kubectl apply -f secret.local.yaml
apiVersion: v1
kind: Secret
metadata:
  name: crochelia-secret
  namespace: crochelia
type: Opaque
stringData:
  SUPABASE_SECRET_KEY: ""
  ANTHROPIC_API_KEY: ""
  DATABASE_URL: ""
